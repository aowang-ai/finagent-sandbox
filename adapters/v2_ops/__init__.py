"""Suite-specific v2 harvest / resume / upstream patches.

EnvAdapters stay in `adapters/{suite}.py` (protocol → invoke → SuiteResult).
Glue that is not the exam-room loop lives here so adapter bodies stay short.
"""
